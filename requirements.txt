# HTTP
requests>=2.28.0

# 数据库
sqlalchemy>=2.0.0
PyMySQL>=1.0.0        # MySQL 驱动 (可选)

# API 服务
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# CLI
typer>=0.9.0

# 配置 / LLM
python-dotenv>=1.0.0
httpx>=0.25.0         # LLM 调用 (ai/comment_analyzer.py)

# 测试
pytest>=7.4.0
httpx>=0.25.0         # FastAPI TestClient 需要

# 说明: 本项目不需要网易那套依赖
# - pyncm / pycryptodome / cryptography: QQ 评论接口是明文 GET, 无加密
# - qrcode / pillow: 无需登录, 不做扫码
# - APScheduler: 暂无定时调度
